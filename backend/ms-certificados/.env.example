# Servidor
PORT=8086

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=seed_educa
DB_USER=seed_user
DB_PASSWORD=seed_pass

# JWT (deve ser o mesmo segredo do ms-autenticacao)
JWT_SECRET=troque-por-um-segredo-forte

# Kafka
KAFKA_BROKERS=localhost:9092
KAFKA_TOPIC=inscricao-concluida
KAFKA_GROUP_ID=ms-certificados

# MinIO
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=seed_minio_user
MINIO_SECRET_KEY=seed_minio_pass
MINIO_BUCKET=certificados
MINIO_USE_SSL=false
