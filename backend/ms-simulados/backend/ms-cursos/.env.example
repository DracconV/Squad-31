# ms-cursos — variáveis de ambiente
# Copie para .env (que está no .gitignore) e ajuste conforme seu setup.

PORT=8085

DB_HOST=localhost
DB_PORT=5432
DB_USER=seed_user
DB_PASSWORD=seed_pass
DB_NAME=seed_educa
DB_SSLMODE=disable
