# ============================================================
# SEED Educa — Variáveis de Ambiente (desenvolvimento local)
# ============================================================
# ATENÇÃO: Este arquivo NUNCA deve ir para o GitHub!
# Ele já está no .gitignore.
# Cada desenvolvedor do squad deve ter o seu próprio.
# ============================================================

# Banco de dados PostgreSQL
POSTGRES_DB=seed_educa
POSTGRES_USER=seed_user
POSTGRES_PASSWORD=seed_pass

# MinIO (armazenamento de arquivos)
MINIO_ROOT_USER=seed_minio_user
MINIO_ROOT_PASSWORD=seed_minio_pass
